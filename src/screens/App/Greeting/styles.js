import { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  button: {
    backgroundColor: "#8FCAFB",
    width: 200,
    height: 50,
    borderRadius: 25,
    elevation: 1
  },
}));
